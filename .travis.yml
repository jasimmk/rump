sudo: required
language: go
go:
  - 1.9.x
  - 1.10.x
services:
  - docker

before_install:
  - curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh
  - dep ensure

script:
  - docker -v
  - go test -v ./